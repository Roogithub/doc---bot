services:
  - type: web
    name: doc---bot
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r main.bot/requirements.txt
    startCommand: python main.bot/main.py
    rootDir: .
